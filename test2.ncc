uint main() {
    while(true){
        char* str[16];
        uint rand=0;
        uint num=0;
        rand=random();
        num = rand % 16;
        int_to_ascii(num,str);
        output_string(str);
        output_string("\n");
    }
    return 0;
}