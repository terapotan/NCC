# NC16C 言語仕様書

NC16C は、NC-16（CPUNC16）アーキテクチャ向けの C 風プログラミング言語です。
本コンパイラ（`nc16cc`）は Python で実装されており、NC16C のソースコードから
NC-16 アセンブリ（customasm 形式、`nc16_assemble_USERPROGRAM.asm` を前提とした
ユーザープログラム）を出力します。

このドキュメントは、コンパイラの実装上の都合や NC-16 のハードウェア制約により
本家 C 言語から変更・簡略化した点をすべて明記することを目的としています。

---

## 1. 全体方針

NC-16 は以下の特性を持つため、これらに合わせて言語仕様を設計しています。

- **ワードアドレッシング**：1アドレス = 1ワード = 16bit。バイト単位のアドレッシングは存在しない。
- **汎用レジスタが極めて少ない**：a, b, c, d, e, bp, sp の7本のみ（bp, sp は規約上フレームポインタ／スタックポインタとして予約）。
- **除算命令が存在しない**：MomoOSの`divide`システムコール（復元法による二進除算）を利用する。
- **左シフト命令が存在しない**：`shr`（右シフト）はあるが`shl`はない。
- **比較命令（jl/jle/ja/jae）は符号フラグのみに基づく符号付き比較である**：`uint`型の0〜65535全域で正しい大小比較を行うため、比較の直前に両オペランドの最上位ビットを`xor 0x8000`で反転させてから比較するテクニックを用いている。
- **syscall命令はbpレジスタを上書きする**（成功時0、不正なシステムコール番号なら1）。本コンパイラはbpをフレームポインタとして使うため、syscallを発行するすべての箇所でbpを退避・復元している。

---

## 2. 型システム

サポートする型は次の3つの基本型と、それらを組み合わせた派生型です。

| 型 | サイズ | 説明 |
|---|---|---|
| `bool` | 1ワード | 0または1 |
| `uint` | 1ワード | 符号なし16bit整数（0〜65535） |
| `char` | 1ワード | 1文字。**実際のCと異なり1ワードを占有する**（下記「Cとの違い」参照） |
| `T*` | 1ワード | Tへのポインタ（アドレス） |
| `T[N]` | Nワード | T型の要素をN個持つ配列 |
| `struct S` | メンバの合計サイズ | 構造体。メンバは宣言順に詰めて配置される（パディングなし） |

`signed int` / `float` / `double` / `long` / `short` は存在しません。

### 2.1 文字列リテラルについて（重要な例外）

上記の通り、ユーザーが宣言する`char`配列は1文字=1ワードとして扱われますが、
**文字列リテラル（`"..."`）だけは特別に、既存のMomoOS/BIOSが使っている
「1ワードに2文字（上位8bit, 下位8bit）＋NULL終端」という詰め込み形式**で
データセクションに配置されます。これは`output_string`等のシステムコールと
実際に相互運用できるようにするための実用上の判断です。

そのため、文字列リテラル由来の`char*`（`output_string("hello")`など）は
そのままシステムコールに渡して問題ありませんが、ユーザーが`char buf[16];`
のように自前で宣言した文字配列は1ワード/文字のレイアウトになっており、
**そのままでは`output_string`等に渡しても正しく表示されません**
（`int_to_ascii`のように、あらかじめOS側のパック形式でバッファに書き込んで
くれるシステムコールの出力バッファとして使うか、文字列リテラルをコピーする
用途にとどめてください）。

---

## 3. 文法（EBNF風の記述）

```
program      := (struct_decl | func_decl | var_decl)*

struct_decl  := "struct" IDENT "{" member_decl* "}" ";"
member_decl  := type IDENT ("[" NUMBER "]")? ";"

func_decl    := type IDENT "(" (param ("," param)* | "void")? ")" block
param        := type IDENT ("[" NUMBER "]")?      // 配列引数はポインタへdecayする

var_decl     := type IDENT ("[" NUMBER "]")? ("=" expr)? ";"
type         := ("char" | "bool" | "uint" | "struct" IDENT) "*"*

block        := "{" stmt* "}"
stmt         := var_decl
              | expr ";"
              | "if" "(" expr ")" stmt ("else" stmt)?
              | "while" "(" expr ")" stmt
              | "do" stmt "while" "(" expr ")" ";"
              | "for" "(" (var_decl | expr ";" | ";") expr? ";" expr? ")" stmt
              | "switch" "(" expr ")" "{" ("case" NUMBER ":" stmt*)* ("default" ":" stmt*)? "}"
              | "break" ";"
              | "continue" ";"
              | "return" expr? ";"
              | block

// 優先順位は下から上に向かって強くなる（Cとほぼ同じ）
expr         := unary "=" expr | unary ("+=" | "-=") expr | or_expr
or_expr      := and_expr ("||" and_expr)*
and_expr     := bitor_expr ("&&" bitor_expr)*
bitor_expr   := bitxor_expr ("|" bitxor_expr)*
bitxor_expr  := bitand_expr ("^" bitxor_expr)*
bitand_expr  := eq_expr ("&" eq_expr)*
eq_expr      := rel_expr (("==" | "!=") rel_expr)*
rel_expr     := shift_expr (("<" | "<=" | ">" | ">=") shift_expr)*
shift_expr   := add_expr (("<<" | ">>") add_expr)*
add_expr     := mul_expr (("+" | "-") mul_expr)*
mul_expr     := unary (("*" | "/" | "%") unary)*
unary        := ("-" | "!" | "~" | "*" | "&" | "++" | "--") unary | postfix
postfix      := postfix "[" expr "]"
              | postfix "." IDENT
              | postfix "->" IDENT
              | postfix "++" | postfix "--"
              | IDENT "(" (expr ("," expr)*)? ")"        // 関数呼び出し
              | primary
primary      := NUMBER | CHARLIT | STRINGLIT | "true" | "false"
              | IDENT | "(" expr ")"
```

---

## 4. Cとの違い（意図的な変更・簡略化）

1. **前処理系（`#include`, `#define`, マクロ）は存在しません。** 単一ファイルとしてコンパイルします。
2. **`char`は1バイトではなく1ワード**として扱います（上記2.1参照）。文字列リテラルのみ例外的に2文字/ワードで詰め込まれます。
3. **配列は真の意味での多次元配列に対応していません。**（構造体の配列や、構造体メンバとしての配列は可能です）
4. **関数プロトタイプ宣言（本体なしの宣言）は未対応**です。関数は使用前に定義するか、コンパイラが2パス方式で全関数シグネチャを事前登録するため、実際にはファイル中どこにあっても相互に呼び出せます（前方参照OK）。
5. **`typedef` / `union` / 関数ポインタ / 可変長引数は非対応**です。
6. **ゼロ除算は未定義動作ではなく、`商=0, 余り=0`となることを言語仕様として定義しています。**（MomoOSの`divide`システムコールの仕様に準拠）
7. **`switch`文はC同様にフォールスルーします。** `break`を書かないと次の`case`に処理が続きます。
8. **`++x` / `x++` の前置・後置の違い（評価結果としての値の違い）は区別していません。** どちらも「その場で1加算する」効果は正しく持ちますが、複合式の中で前置後置の評価順序の違いに依存したコードは書かないでください。
9. **グローバル変数の初期化式は定数（数値リテラルおよび true/false）のみ**サポートします。配列・構造体型のグローバル変数は初期化できません（ゼロ初期化領域として確保されます）。
10. **ポインタ同士の減算はポインタが指す型のサイズで割る、というC言語の挙動を実装していません。**単純なワード差分になります。
11. **構造体のメモリレイアウトにパディングはありません。** すべて1ワード単位で詰めて配置されます。

---

## 5. メモリモデル・呼び出し規約

### 5.1 スタックフレーム

関数呼び出しは、引数を**右から左へ**スタックにpushするcdecl風の規約を採用しています。

```
呼び出し側:
    第N引数を評価してpush
    ...
    第1引数を評価してpush
    call 関数名
    add sp, 引数の数           ; 呼び出し側でスタックを片付ける

関数プロローグ:
    push bp
    mov bp, sp
    sub sp, ローカル変数の合計ワード数   ; 必要な場合のみ

関数エピローグ:
    mov sp, bp
    pop bp
    ret
```

呼び出された関数の中からは、次のオフセットで引数・ローカル変数にアクセスします。

| 位置 | 内容 |
|---|---|
| `bp+0` | 呼び出し元のbp（保存済み） |
| `bp+1` | 戻り番地 |
| `bp+2` | 第1引数 |
| `bp+3` | 第2引数 |
| ... | ... |
| `bp-1`, `bp-2`, ... | ローカル変数（宣言順に、配列/構造体はサイズ分まとめて確保） |

### 5.2 戻り値

関数の戻り値は常に `a` レジスタで返されます。`void`関数（本コンパイラでは戻り値の型を
`void`とした関数）はaレジスタの値は不定として扱ってください。

### 5.3 式の評価方式

NC-16はレジスタが非常に少ないため、複雑な部分式の一時的な値は**すべてハードウェア
スタック（push/pop）に退避**する方式でコード生成しています。これはレジスタ割り付けの
複雑さを避け、正しさを最優先するための設計判断です（速度面では最適化の余地が
大きく残されています）。

---

## 6. 演算子とNC-16命令の対応

| 演算子 | 実装方法 |
|---|---|
| `+` `-` `*` `&` `\|` `^` | NC-16の`add`/`sub`/`mul`/`and`/`or`/`xor`命令に直接対応 |
| `~` | `not`命令 |
| `>>` | `shr`命令（レジスタ指定シフト数に対応） |
| `<<` | NC-16に左シフト命令が無いため、`add a,a`（2倍）をシフト量の回数だけ繰り返すループとして実装 |
| `/` `%` | MomoOSの`divide`システムコール（番号6）を直接呼び出し、商／余りを取得 |
| `==` `!=` | `cmp`＋`je`/`jne`（符号の影響を受けないためそのまま使用可能） |
| `<` `<=` `>` `>=` | 両オペランドを`xor 0x8000`してから`cmp`＋`jl`/`jle`/`ja`/`jae`（符号付き比較器で符号なし比較を正しく行うためのビット変換テクニック） |
| `&&` `\|\|` | 短絡評価（ラベルによる分岐） |
| `&x`（アドレス取得） | 対象のlvalueのアドレス計算のみ行い、参照は行わない |
| `*x`（間接参照） | `mov memaddr,x` → `mov a,[memaddr+0]` |
| ポインタ演算 (`p + n`, `p - n`) | `n`を指す先の型のサイズ倍してから加減算（配列のインデックス計算と同じ考え方） |

---

## 7. 組み込み関数（MomoOSシステムコールのラッパー）

以下の7つの関数名は予約されており、ユーザーが同名の関数を定義することはできません。
自動的にMomoOSの対応するシステムコールを呼び出すコードが生成されます
（レジスタの受け渡しはコンパイラが自動的に行うため、ユーザーはレジスタを意識する
必要はありません）。

| 関数シグネチャ | 説明 |
|---|---|
| `uint input_user_string(char* buf);` | キーボード入力を取得。戻り値: 0=成功, 1=権限エラー |
| `uint compare_to_string(char* s1, char* s2);` | 文字列比較。戻り値: 1=等しい, 0=等しくない, 2=権限エラー |
| `uint read_rom_data(uint rom_io_addr, uint src_addr, uint length_words, uint dest_addr);` | ROMからのデータ読み込み。戻り値: 0=成功, 1=権限エラー |
| `uint output_string(char* str);` | 文字列を画面に表示。戻り値: 0=成功, 1=権限エラー |
| `uint ascii_to_int(char* str, uint* out_value);` | 数字文字列→数値変換。戻り値: 0=成功,1=不正文字,2=空文字列,3=権限エラー。変換結果は`out_value`へ書き込まれる |
| `uint int_to_ascii(uint value, char* buf);` | 数値→数字文字列変換。戻り値: 0=成功,1=権限エラー |
| `uint divide(uint dividend, uint divisor, uint* remainder, uint* status);` | 除算。戻り値: 商。余りは`remainder`へ、状態(0=成功,1=ゼロ除算)は`status`へ書き込まれる |

`out_value` / `remainder` / `status` のようなポインタ引数には、ローカル変数の
アドレス（`&x`）を渡してください。値が不要な場合でも、ダミーの変数のアドレスを
渡す必要があります（C言語の`NULL`を渡すような省略はできません）。

`/` および `%` 演算子は、内部的にこの`divide`をそのまま呼び出しています。

---

## 8. 使い方

### 8.1 必要なライブラリ

本コンパイラは字句解析・構文解析に **ply**（Python Lex-Yacc）を使用しています。
以下のコマンドでインストールしてください。

```
pip install ply
```

### 8.2 コンパイル方法

```
python3 compiler.py 入力ファイル.c16 -o 出力ファイル.asm
```

出力される`.asm`ファイルは、`#include "nc16_assemble_USERPROGRAM.asm"`から始まる
customasm形式のユーザープログラムです。`program_start`ラベルから実行が始まり、
`main()`関数を呼び出した後`userret`でOSに処理を戻します。そのため、通常の
ユーザープログラムと同様に、customasmでのアセンブル・MomoOSからのロードによって
実行できます。

### 8.3 テスト用の簡易エミュレータ

`nc16emu.py` は、生成したアセンブリのロジック（制御フロー・スタック操作・
算術演算・システムコールとのレジスタ受け渡し）を、実機やLogisim-evolutionを
使わずにこの場で検証するための簡易Pythonエミュレータです。**サイクル精度の
シミュレータではなく、あくまでコンパイラ開発時のロジック検証用のツール**である
点に注意してください（命令のアドレス割り当て方式も実機の8バイト命令とは異なる
単純化を行っています）。実機での最終確認は、別途customasm＋Logisim-evolution等
で行ってください。

```python
from nc16emu import Emulator
emu = Emulator(open("出力ファイル.asm", encoding="utf-8").read())
emu.run()
print(emu.regs, emu.output)
```

`verify_stack.py` は、生成されたアセンブリの各関数についてpush/popの深さが
どの実行経路でも一致するか（スタック破壊が起きていないか）を静的に検証する
補助ツールです。

```
python3 verify_stack.py 出力ファイル.asm
```

---

## 9. 既知の制限・今後の課題

- 式評価が常にスタック経由のため、実行速度は最適化されていません（レジスタ割り付けの
  最適化は今後の課題です）。
- エラーメッセージの位置情報（行番号）が一部の意味解析エラーで欠落しています。
- 配列の境界チェックは行いません（範囲外アクセスは未定義動作です）。
- `const`修飾子はありません。
- 文字列リテラルの重複除去は行いますが、同一内容でも別々の文字列オブジェクトとして
  扱いたい場合の区別はできません。
