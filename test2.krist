uint add(uint a,uint b){
    return (a + b * 2) / 10;
}

char output_str[16];



uint main() {
    uint i=0;
    char a[16];
    char b[16];
    uint c[5];
    c[0]=200;
    c[1]=8;
    c[2]=2;
    c[3]=6;
    c[4]=3;
    uint a_val=0;
    uint b_val=0;
    uint sum = 0;
    uint average = 0;


    // input_user_string(&a);
    // input_user_string(&b);
    // ascii_to_int(a,&a_val);
    // ascii_to_int(b,&b_val);

    for (i = 0; i < 5; i++) {
        sum = sum + c[i];
    }

    int_to_ascii(sum,output_str);
    output_string(output_str);
    output_string("\n");

    if ( sum < 50 ) {
        output_string("Small!");
    } else {
        output_string("Bigger!");
    }


    //int_to_ascii(add(a_val,b_val),output_str);
    //output_string(output_str);


//プログラミング言語の名前、NCシリーズのC言語と言う意味でNCCにしよう。

    output_string("\n");
    return 0;
}